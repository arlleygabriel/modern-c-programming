#include <stdio.h>

int main (void){
  int ret = 0; 

  ret = printf("papobinario.com.br\n"); //the printf return is the char number for the string
  printf("The return from the previous printf() is : %d\n", ret);
  return 0;
}
