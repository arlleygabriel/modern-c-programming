#include "/usr/include/stdio.h" //include using the stdio.h dir
// #include <stdio.h> with <> says that archive there is at the default location in that case is /usr/include
int main (void){
  printf("papobinario.com.br\n");
  printf("vejo voces na aula1\n");
  return 0;
 }
